# Example environment for a single client deployment

# Each client can have its own subdomain and storage
BASE_DOMAIN=clienta.emitit.local
STORAGE_CLASS=standard
TIMEOUT=10m

# Optional extras you might add later (not yet used but useful)
 MYSQL_DATABASE=clientadb
 MYSQL_ROOT_PASSWORD=someStrongPassword
 REDIS_PASSWORD=random123
 CUSTOM_PORT=8080
